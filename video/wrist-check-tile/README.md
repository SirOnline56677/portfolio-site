# Wrist Check tile video (Remotion source)

Source for `public/work/wrist-check-a-peer-to-peer-market-place/tile-wordmark.mp4`
(and the matching poster PNG): the flip-dot mark ticking over the boutique
image — an 88-frame seamless loop (hour hand parked NW, minute orbiting).

```bash
npm install
npm run render   # writes tile-wordmark.mp4 into the site's public folder
npm run still    # writes the poster frame (10:10 pose lands at frame 11)
```
